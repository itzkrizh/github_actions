name: 04 -  Using Actions

on: push

jobs:
  build:
    runs-on:ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Printing Folders
        run: ls

      - name: Setup Node.js
        uses: actions/setup-node@v3

    #0. check the code from the repo
    # 1. Install deps of our react app
    #2. Run automated tests